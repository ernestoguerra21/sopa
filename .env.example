# PostgreSQL (Supabase → Settings → Database → Session pooler URI)
DATABASE_URL="postgresql://postgres.xxxx:PASSWORD@aws-0-eu-central-1.pooler.supabase.com:5432/postgres"

# API (Render la genera sola con render.yaml)
JWT_SECRET="cambia-esto-en-produccion"

# Web (Vercel): URL del API en Render
API_URL="https://sopa-api.onrender.com"
